'use client';

// Placeholder — D1 migrations are applied via wrangler, not node-postgres.
// See docs/migration/d1.md

console.log(
  'Use: npx wrangler d1 migrations apply the-ants-db --local  (or --remote)\n' +
    'Migration SQL lives in packages/db/drizzle-d1/'
);
