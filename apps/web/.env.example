# The ANTS — Web app environment (copy to apps/web/.env.local)
# Full stack reference: ../../.env.example

# Required — Hono API base URL
NEXT_PUBLIC_API_URL=http://127.0.0.1:8787

# Optional — Telegram login widget
# NEXT_PUBLIC_TELEGRAM_BOT_USERNAME=TheANTS_bot
